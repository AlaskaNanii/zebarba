import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RegistrobarbeiroPage } from './registrobarbeiro.page';

describe('RegistrobarbeiroPage', () => {
  let component: RegistrobarbeiroPage;
  let fixture: ComponentFixture<RegistrobarbeiroPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RegistrobarbeiroPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
